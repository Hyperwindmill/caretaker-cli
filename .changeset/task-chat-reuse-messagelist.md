---
"caretaker-cli": patch
"webview-ui": patch
---

Task chat now reuses the normal chat renderer (MessageList): markdown, thinking, and tool blocks render consistently with the interactive chat instead of raw plain text.
