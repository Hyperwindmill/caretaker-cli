---
'@hyperwindmill/caretaker-cli': patch
---

Design spec and implementation plan for memory subsystem step 3: the recall/read path (host-side inverted lexical keyword match on the user message, `<memories>` prelude block with top-K titles, `mcp__memory__memory_read` tool with recall accounting via `recallCount`/`lastRecalledAt`).
