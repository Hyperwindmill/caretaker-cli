---
'caretaker-cli': minor
---

Add native `read_document` tool to parse PDF, Word, and Excel files with pandoc fallback for unsupported formats. Add native `read_image` tool along with support for pointer-based tool attachments mapped directly to base64 user messages in LLM turns.
