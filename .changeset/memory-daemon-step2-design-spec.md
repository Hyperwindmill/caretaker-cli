---
'@hyperwindmill/caretaker-cli': patch
---

Design spec for memory subsystem step 2: memory extraction from the sweep's per-chunk call (combined summary+memories JSON output, project/global scope, kind+importance classification, durable `memories` collection).
