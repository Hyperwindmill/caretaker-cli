node_modules
.pnpm-store
dist
*.log
.DS_Store
*.vsix
.atl

# Secrets / local env
.env
.env.*
!.env.example
*.local

# Test coverage
coverage

# Local caretaker state (in case CARETAKER_HOME points inside the tree)
.caretaker

# Electron build outputs
dist-electron
release
# Playwright MCP scratch (page snapshots, console logs) from driving the local web GUI
.playwright-mcp
